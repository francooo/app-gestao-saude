import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function AssistenteScreen() {
  return (
    <PlaceholderScreen
      icone="message-square"
      titulo="Assistente de Saúde"
      descricao="Ainda em construção. Antes de liberar, precisamos definir com cuidado o que o assistente pode responder — orientação sobre medicamentos exige limites claros."
    />
  );
}
