# O Metro (bundler do Expo) nao resolve symlinks aninhados do pnpm por padrao.
# node-linker=hoisted deixa o node_modules em formato plano, como o npm.
# Recomendacao oficial do Expo para monorepos pnpm.
node-linker=hoisted
